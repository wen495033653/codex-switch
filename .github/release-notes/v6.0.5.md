## v6.0.5

- Token 统计改为使用 Codex 记录的官方用量逐次计算，分叉会话和子任务的用量不再漏算；会话中途切换账号后，按每次请求实际使用的账号计入。
- 费用按每次请求单独计算，区分快速模式和长上下文；价格表更新为最新官方价格，新增 GPT-6 系列模型。
- 升级前的统计数据保留，并按最新价格重新计算费用。
- 大幅降低后台磁盘读取：会话同步检查和 Token 统计刷新不再反复读取全部会话文件。
- 修复结束 Codex 时偶发被误判为结束失败的问题。
