{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main window.",
  "windows": ["main", "dev-log"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
